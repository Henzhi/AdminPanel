# 文化遗产数字平台——后台管理子系统综合实验报告

| 项目名称  | 文化遗产数字平台——后台管理子系统 |
| ----- | ----------------- |
| 学院/专业 | 计算机科学与技术          |
| 指导教师  | <br />            |
| 实验学生  | <br />            |
| 实验日期  | 2026年6月           |

***

## 摘要

本报告详细记录了"文化遗产数字平台——后台管理子系统"的完整开发过程。本系统采用前后端分离架构，后端基于 Spring Boot 3 + MyBatis-Plus 构建 RESTful API，前端基于 Vue 3 + Element Plus 实现交互界面，通过 Docker 容器化部署。系统实现了基于 RBAC 的角色权限管理、文物数据管理、知识图谱三元组管理、数据备份恢复以及三类日志（操作/安全/系统）的记录与查询功能。在开发过程中，全程借助 AI 辅助工具（GLM-5.2）进行需求分析、架构设计、代码编写与问题调试，显著提升了开发效率。本报告详细阐述 AI 在软件开发各环节中的实际应用、优势与局限性。

**关键词**：文化遗产；后台管理系统；Spring Boot；Vue 3；RBAC；AI 辅助开发；Docker

***

## 第一章 项目概述

### 1.1 项目背景

随着数字化技术在文化遗产保护领域的深入应用，越来越多的博物馆、文化机构开始建设数字资源管理系统。文化遗产包含文物、古籍、遗址等多种形态，其数字化管理涉及数据存储、权限控制、安全备份、审计追踪等多方面需求，对后台管理系统的功能完整性与稳定性提出了较高要求。

传统单体式后台管理系统在面对多角色协同、数据量大、安全性要求高等场景时，存在扩展性差、部署困难、维护成本高等问题。本项目旨在构建一个功能完善、安全可靠、易于维护的文化遗产数字平台后台管理子系统，采用当前主流的微服务化开发模式（前后端分离 + 容器化部署），为文化遗产的数字化管理提供技术支撑。

### 1.2 开发目标

本项目的核心开发目标如下：

1. **角色权限管理**：基于 RBAC（Role-Based Access Control）模型实现细粒度的权限控制，支持超级管理员、内容审核员、数据管理员三类角色的动态菜单生成与权限实时校验。
2. **文物数据管理**：实现文物数据的增删改查、分页搜索、批量导入导出（CSV）、图片上传与预览等完整功能。
3. **知识图谱管理**：以 MySQL 关系表模拟三元组（主体-关系-客体）的存储与编辑，提供图数据库同步的模拟接口。
4. **数据备份与恢复**：支持手动全量备份（mysqldump）、AES 加密备份、定时自动备份、备份记录管理与数据库恢复（需超级管理员二次确认）。
5. **日志管理与审计**：通过 AOP 切面自动采集操作日志、安全日志与系统日志，提供多维度查询与 CSV 导出功能。
6. **容器化部署**：通过 Docker + Docker Compose 实现一键部署，包含后端服务、MySQL 数据库、Redis 缓存与前端 Nginx 服务。

***

## 第二章 需求分析

### 2.1 功能需求

#### 2.1.1 用例图

```
┌─────────────────────────────────────────────────────────┐
│                     后台管理系统                         │
│                                                        │
│  ┌──────────┐                                          │
│  │ 超级管理员 │──┬── 管理管理员账号                      │
│  └──────────┘  ├── 管理普通用户                         │
│                 ├── 角色权限分配                         │
│                 ├── 备份恢复操作                         │
│                 ├── 查看全部日志                         │
│                 └── 系统配置                            │
│                                                        │
│  ┌──────────┐                                          │
│  │ 内容审核员 │──┼── 查看文物数据                        │
│  └──────────┘  ├── 查看知识图谱                         │
│                 └── 查看操作/安全日志                    │
│                                                        │
│  ┌──────────┐                                          │
│  │ 数据管理员 │──┼── 增删改查文物数据                    │
│  └──────────┘  ├── 管理知识图谱                         │
│                 ├── 查看备份记录/下载                    │
│                 └── 查看操作日志                        │
└─────────────────────────────────────────────────────────┘
```

#### 2.1.2 用例描述表

|  用例编号 | 用例名称    | 参与者         | 用例描述                      | 前置条件                | 后置条件                            |
| :---: | ------- | ----------- | ------------------------- | ------------------- | ------------------------------- |
| UC-01 | 管理员登录   | 管理员         | 管理员输入用户名、密码和验证码进行登录       | 管理员账号已存在且状态正常       | 系统返回 Token，后续请求携带 Token 访问受保护资源 |
| UC-02 | 管理管理员账号 | 超级管理员       | 超级管理员对管理员账号进行增删改查和密码重置    | 已以超级管理员身份登录         | 管理员账号数据正确更新，操作日志已记录             |
| UC-03 | 管理普通用户  | 超级管理员       | 超级管理员对前台注册用户进行增删改查和密码重置   | 已以超级管理员身份登录         | 普通用户数据正确更新，操作日志已记录              |
| UC-04 | 角色权限分配  | 超级管理员       | 超级管理员为角色分配或撤销权限节点         | 已以超级管理员身份登录         | 权限变更实时生效，操作日志已记录                |
| UC-05 | 管理文物数据  | 内容审核员、数据管理员 | 对文物数据进行增删改查、批量导入导出、图片上传   | 拥有相应权限              | 文物数据正确更新，导入/导出文件正确生成            |
| UC-06 | 管理知识图谱  | 数据管理员       | 对三元组数据进行增删改查操作            | 拥有相应权限              | 知识图谱数据正确更新                      |
| UC-07 | 手动备份    | 数据管理员       | 触发全量数据库备份，可选择是否 AES 加密    | 拥有 backup:create 权限 | 备份文件生成，备份记录入库                   |
| UC-08 | 恢复备份    | 超级管理员       | 选择备份记录恢复数据库，需二次密码确认       | 已以超级管理员身份登录         | 数据库恢复到选定备份点                     |
| UC-09 | 查看操作日志  | 全部角色        | 按操作人、类型、关键词、时间范围多条件查询操作日志 | 拥有 log:operation 权限 | 符合条件的日志记录分页展示                   |
| UC-10 | 查看安全日志  | 全部角色        | 查询登录、改密、权限变更等安全相关操作记录     | 拥有 log:security 权限  | 符合条件的日志记录展示                     |
| UC-11 | 查看系统日志  | 超级管理员       | 查询系统启动、定时任务、备份恢复等系统事件记录   | 拥有 log:system 权限    | 符合条件的日志记录展示                     |
| UC-12 | 修改密码    | 全部管理员       | 修改当前登录账号的密码               | 已登录                 | 密码更新，旧 Token 失效                 |

### 2.2 非功能需求

#### 2.2.1 性能需求

- 页面首次加载时间不超过 3 秒（网络正常条件下）
- API 响应时间：简单查询接口 P95 < 200ms，复杂查询接口 P95 < 500ms
- 支持不少于 100 个并发用户同时在线
- 日志导出功能需在 10 秒内完成 1000 条记录的 CSV 生成

#### 2.2.2 安全性需求

- 所有敏感接口强制身份认证（Sa-Token JWT Token）
- 管理员密码 BCrypt 加密存储（强度 10）
- 敏感字段（password、token）在日志中自动脱敏
- SQL 注入防护：全站使用 MyBatis-Plus 参数化查询
- 备份恢复需超级管理员二次密码确认
- 数据库连接信息不硬编码，通过环境变量或配置文件注入

#### 2.2.3 易用性需求

- 界面使用 Element Plus 组件库，保持风格一致
- 所有操作配备加载状态提示（loading）和结果反馈（Message）
- 危险操作（删除、恢复）配备确认对话框
- 表单提交前进行客户端校验（Element Plus Form Validation）
- 支持移动端基本可用（响应式布局）

#### 2.2.4 可维护性需求

- RESTful API 规范设计，接口文档清晰
- 代码分层清晰：Controller → Service → Mapper / Entity
- 统一异常处理（GlobalExceptionHandler）
- 统一的 JSON 响应格式（Result 包装类）
- 日志记录到文件（logback），便于问题排查
- Docker 容器化部署，一键启动

#### 2.2.5 可扩展性需求

- RBAC 权限模型支持灵活的角色与权限节点扩展
- 前端动态菜单支持新增页面菜单节点后自动渲染
- 数据库支持通过增量迁移脚本（migration\_\*.sql）扩展表结构

### 2.3 AI 辅助需求分析

#### 2.3.1 使用的 AI 工具

本项目全程使用 **GLM-5.2** 大语言模型（智谱 AI）作为辅助开发工具，集成于 Trae IDE 中。

#### 2.3.2 典型 Prompt 示例

**示例 1：生成需求分析框架**

```
Prompt: 我需要为一个文化遗产数字平台后台管理子系统做需求分析。请帮我：
1. 识别核心功能模块（基于"管理员管理、文物管理、备份恢复、日志管理"四大模块）
2. 为每个模块列出主要用例
3. 确定非功能需求（性能、安全、易用性等）
4. 设计角色权限矩阵（超级管理员、内容审核员、数据管理员）
输出格式：Markdown 表格
```

**AI 输出摘要**：AI 识别出 6 个功能模块，输出了完整的用例描述表和 RBAC 权限矩阵表格。

**人工修改内容**：

- 补充了"普通用户管理"模块（原需求中未明确提及，但实际业务需要）
- 根据项目实际情况调整了权限矩阵中的具体权限节点
- 补充了系统日志与安全日志的具体字段定义

**示例 2：细化日志采集方案**

```
Prompt: 系统需要记录三类日志：操作日志、安全日志、系统日志。请设计：
1. 统一的日志表结构（log_type 字段区分类型）
2. 日志记录的方式（手动记录 vs AOP 自动记录）
3. 日志查询接口设计
4. 日志保留策略

现有上下文：使用 Spring Boot + MyBatis-Plus
```

**AI 输出摘要**：AI 给出了单表 + log\_type 区分的设计方案，并建议使用 @Around AOP 切面 + @OperLog 自定义注解实现自动采集。

**人工修改内容**：

- 调整了 log\_type 字段名与数据库表字段名一致
- 补充了异步持久化（@Async）以避免阻塞业务请求
- 添加了 WebSocket 实时推送日志通知到仪表盘的需求

***

## 第三章 系统设计

### 3.1 系统架构设计

#### 3.1.1 整体架构

本系统采用前后端分离架构，前端为单页应用（SPA），后端为 RESTful API 服务，通过 HTTP JSON 通信。部署层面使用 Docker Compose 实现容器化，所有服务共享宿主机内核。

```
┌────────────────────────────────────────────────────────────────────┐
│                           用户浏览器                                 │
│                    http://localhost:8000                           │
└───────────────────────────┬────────────────────────────────────────┘
                            │ HTTP/HTTPS
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│   Nginx       │  │   MySQL      │  │   Redis       │
│  (Frontend)   │  │   3306       │  │   6379       │
│   Port: 8000  │  │  Port: 3307  │  │  Port: 6380  │
└───────────────┘  └───────────────┘  └───────────────┘
        │                                       │
        │ HTTP (Proxy)                          │ Session/Redis
        ▼                                       ▼
┌────────────────────────────────────────────────────────────────────┐
│                        relic-backend                                │
│                      Spring Boot 3 + MyBatis-Plus                   │
│                    Sa-Token 认证 + WebSocket 通知                   │
│                       Port: 8081 (容器内 8080)                     │
│                                                                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │Controller层 │  │ Service层  │  │ Mapper层   │  │  Entity层  │  │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘  │
│                                                                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │  AOP切面   │  │ 过滤器/拦截器│  │ 配置类    │  │  WebSocket │  │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 技术选型说明

| 层次     | 技术选型                         | 选型理由                               |
| ------ | ---------------------------- | ---------------------------------- |
| 后端框架   | Spring Boot 3.2 + Spring MVC | 主流企业级框架，生态完善，支持 Jakarta EE         |
| ORM    | MyBatis-Plus 3.5             | Lambda QueryWrapper 简化 CRUD，分页插件强大 |
| 认证授权   | Sa-Token 1.37                | 轻量级，支持 Redis 存储，注解驱动，文档完善          |
| 数据库    | MySQL 8 (Docker)             | 开源关系型数据库，稳定可靠，Docker 易部署           |
| 缓存     | Redis (Docker)               | Saa-Token Session 存储，图形验证码存储       |
| 前端框架   | Vue 3 + Composition API      | 主流前端框架，组合式 API 灵活组合逻辑              |
| UI 组件库 | Element Plus 2.5+            | Vue 3 官方推荐组件库，主题定制能力强              |
| 构建工具   | Vite 6                       | 基于 ESM 的下一代构建工具，速度极快               |
| 图表     | ECharts                      | 百度开源图表库，功能丰富，文档完善                  |
| 部署     | Docker + Docker Compose      | 容器化标准化部署，一键启动全量环境                  |
| 图片存储   | 阿里云 OSS（SDK 集成）              | 海量存储、CDN 分发，URL 直连                 |
| CSV 处理 | OpenCSV                      | 轻量级 Java CSV 处理库                   |
| 加密     | AES (javax.crypto)           | Java 原生对称加密，备份文件加密                 |

### 3.2 模块设计

#### 3.2.1 后端模块结构

```
backend/src/main/java/com/relic/admin/
├── config/           # 配置类
│   ├── AsyncConfig.java      # 异步线程池配置（@EnableAsync）
│   ├── WebSocketConfig.java  # WebSocket 配置
│   └── CorsConfig.java       # 跨域配置
├── controller/      # REST 控制器层
│   ├── AuthController.java       # 认证（登录/登出/改密/验证码）
│   ├── AdminController.java       # 管理员管理
│   ├── UserController.java       # 普通用户管理
│   ├── RoleController.java       # 角色管理
│   ├── ArtifactController.java   # 文物管理
│   ├── KnowledgeGraphController.java  # 知识图谱
│   ├── BackupController.java      # 备份恢复
│   └── LogController.java         # 日志查询
├── service/         # 业务逻辑层（接口 + 实现）
│   ├── impl/
│   │   ├── AuthServiceImpl.java
│   │   ├── SysAdminServiceImpl.java
│   │   ├── SysUserServiceImpl.java  # 普通用户服务
│   │   ├── BackupServiceImpl.java   # mysqldump/mysql 调用
│   │   └── SysLogServiceImpl.java   # 异步日志持久化
│   └── SysLogService.java
├── mapper/          # 数据访问层
│   ├── SysAdminMapper.java        # 含角色关联查询
│   ├── SysUserMapper.java
│   ├── SysRoleMapper.java
│   ├── SysPermissionMapper.java
│   ├── ArtifactMapper.java
│   ├── KnowledgeGraphMapper.java
│   ├── BackupRecordMapper.java
│   └── SysLogMapper.java          # 含分页 XML 查询
├── entity/          # 数据库实体
│   ├── SysAdmin.java, SysUser.java, SysRole.java
│   ├── SysPermission.java, SysRolePermission.java
│   ├── Artifact.java, KnowledgeGraph.java
│   ├── BackupRecord.java, SysLog.java
├── dto/            # 数据传输对象
│   ├── LoginDTO.java, PasswordDTO.java
│   ├── AdminDTO.java, UserDTO.java, RoleDTO.java
│   ├── ArtifactDTO.java, KnowledgeGraphDTO.java
│   ├── BackupQueryDTO.java, RestoreDTO.java, LogQueryDTO.java
├── vo/             # 视图对象
│   ├── LoginVO.java, AdminVO.java, UserVO.java
│   ├── PermissionVO.java (树形结构)
│   ├── BackupRecordVO.java, SysLogVO.java
│   └── MenuVO.java (动态菜单树)
├── aop/
│   └── OperationLogAspect.java   # @OperLog AOP 切面
├── annotation/
│   └── OperLog.java             # 自定义日志注解
├── common/
│   ├── Result.java              # 统一响应包装
│   ├── ResultCode.java         # 状态码枚举
│   ├── PageResult.java          # 分页结果包装
│   ├── BusinessException.java   # 业务异常
│   ├── Constants.java           # 系统常量
│   └── AdminContextHolder.java  # 当前登录管理员上下文
├── websocket/
│   └── LogWebSocket.java       # WebSocket 实时日志推送
├── util/
│   ├── IpUtil.java              # IP 地址获取工具
│   └── AesUtil.java             # AES 加解密工具
├── properties/
│   └── BackupProperties.java    # 备份配置属性类
└── init/
    └── DataInitializer.java      # 启动时初始化管理员密码哈希
```

#### 3.2.2 前端模块结构

```
frontend/src/
├── api/               # API 请求封装
│   ├── request.js      # Axios 实例（拦截器）
│   ├── auth.js         # 登录/登出/验证码
│   ├── admin.js        # 管理员管理
│   ├── user.js         # 普通用户管理
│   ├── role.js         # 角色权限
│   ├── artifact.js     # 文物管理
│   ├── knowledge.js    # 知识图谱
│   ├── backup.js       # 备份管理
│   └── log.js          # 日志查询
├── views/
│   ├── Dashboard/      # 仪表盘（ECharts 统计）
│   ├── Artifact/       # 文物管理（含导入导出）
│   ├── Knowledge/      # 知识图谱三元组
│   ├── Backup/         # 备份管理
│   ├── Log/            # 三类日志页面
│   │   ├── Operation.vue   # 操作日志
│   │   ├── Security.vue     # 安全日志
│   │   └── System.vue       # 系统日志
│   ├── System/         # 系统管理
│   │   ├── Admin.vue    # 管理员管理
│   │   ├── Role.vue     # 角色权限
│   │   └── User.vue     # 普通用户管理
│   └── Login.vue        # 登录页
├── router/index.js    # 路由配置（静态 + 动态菜单合并）
├── utils/format.js    # 格式化工具函数
└── stores/             # Pinia 状态管理（预留）
```

### 3.3 数据库设计

#### 3.3.1 ER 图

```
┌─────────────┐       ┌──────────────────┐       ┌─────────────────┐
│  sys_role   │──1:N──│  sys_admin      │       │    sys_user     │
│ (角色表)    │       │ (管理员表)        │       │  (普通用户表)    │
└─────────────┘       └──────────────────┘       └─────────────────┘
       │
       │ N:M
       ▼
┌─────────────────────┐       ┌──────────────────┐
│ sys_role_permission │──N:1─│ sys_permission    │
│ (角色权限关联表)     │       │ (权限表)          │
└─────────────────────┘       └──────────────────┘
                                      │
                                      │ 1:N
                                      ▼
        ┌──────────────┐  ┌─────────────────┐  ┌──────────────┐
        │  artifact    │  │ knowledge_graph │  │ backup_record │
        │ (文物表)      │  │  (知识图谱表)    │  │ (备份记录表)  │
        └──────────────┘  └─────────────────┘  └──────────────┘
                                      │
                                      ▼
                            ┌──────────────────┐
                            │    sys_log       │
                            │    (日志表)       │
                            └──────────────────┘
```

#### 3.3.2 核心表结构

**sys\_admin（管理员表）**

| 字段名               | 类型           | 约束                  | 说明          |
| ----------------- | ------------ | ------------------- | ----------- |
| id                | BIGINT       | PK, AUTO\_INCREMENT | 主键          |
| username          | VARCHAR(50)  | UNIQUE, NOT NULL    | 用户名         |
| password          | VARCHAR(100) | NOT NULL            | BCrypt 哈希密码 |
| real\_name        | VARCHAR(50)  | <br />              | 真实姓名        |
| email             | VARCHAR(100) | <br />              | 邮箱          |
| phone             | VARCHAR(20)  | <br />              | 手机号         |
| avatar            | VARCHAR(255) | <br />              | 头像 URL      |
| role\_id          | BIGINT       | FK → sys\_role.id   | 角色 ID       |
| status            | TINYINT      | NOT NULL, DEFAULT 1 | 1=正常, 0=禁用  |
| last\_login\_time | DATETIME     | <br />              | 最后登录时间      |
| last\_login\_ip   | VARCHAR(50)  | <br />              | 最后登录 IP     |
| create\_time      | DATETIME     | <br />              | 创建时间        |
| update\_time      | DATETIME     | <br />              | 更新时间        |
| deleted           | TINYINT      | NOT NULL, DEFAULT 0 | 逻辑删除        |

**sys\_permission（权限表）**

| 字段名         | 类型           | 约束                  | 说明                       |
| ----------- | ------------ | ------------------- | ------------------------ |
| id          | BIGINT       | PK, AUTO\_INCREMENT | 主键                       |
| parent\_id  | BIGINT       | NOT NULL, DEFAULT 0 | 父权限 ID（0=根节点）            |
| perm\_code  | VARCHAR(100) | UNIQUE, NOT NULL    | 权限编码（resource:action 格式） |
| perm\_name  | VARCHAR(50)  | NOT NULL            | 权限显示名称                   |
| perm\_type  | TINYINT      | NOT NULL            | 1=菜单, 2=按钮               |
| path        | VARCHAR(200) | <br />              | 前端路由路径                   |
| component   | VARCHAR(200) | <br />              | 前端组件路径                   |
| icon        | VARCHAR(50)  | <br />              | 菜单图标                     |
| sort\_order | INT          | NOT NULL, DEFAULT 0 | 排序号                      |

**sys\_log（统一日志表）**

| 字段名               | 类型           | 约束                  | 说明                           |
| ----------------- | ------------ | ------------------- | ---------------------------- |
| id                | BIGINT       | PK, AUTO\_INCREMENT | 主键                           |
| log\_type         | VARCHAR(20)  | NOT NULL            | OPERATION/SYSTEM/SECURITY    |
| operator\_id      | BIGINT       | <br />              | 操作人 ID                       |
| operator\_name    | VARCHAR(50)  | <br />              | 操作人名称                        |
| operation\_type   | VARCHAR(20)  | <br />              | CREATE/UPDATE/DELETE/LOGIN 等 |
| operation\_target | VARCHAR(100) | <br />              | 操作目标（表名或模块名）                 |
| method            | VARCHAR(200) | <br />              | Java 方法全名                    |
| request\_url      | VARCHAR(500) | <br />              | 请求 URL                       |
| request\_method   | VARCHAR(10)  | <br />              | HTTP 方法                      |
| request\_params   | TEXT         | <br />              | 请求参数（JSON，脱敏）                |
| before\_data      | TEXT         | <br />              | 操作前数据（JSON）                  |
| after\_data       | TEXT         | <br />              | 操作后数据（JSON）                  |
| ip                | VARCHAR(50)  | <br />              | 请求 IP 地址                     |
| status            | TINYINT      | NOT NULL, DEFAULT 1 | 1=成功, 0=失败                   |
| error\_msg        | TEXT         | <br />              | 错误信息                         |
| cost\_time        | BIGINT       | <br />              | 耗时（毫秒）                       |
| create\_time      | DATETIME     | <br />              | 创建时间                         |

**artifact（文物表）**

| 字段名          | 类型            | 约束                  | 说明                |
| ------------ | ------------- | ------------------- | ----------------- |
| id           | BIGINT        | PK, AUTO\_INCREMENT | 主键                |
| name         | VARCHAR(100)  | NOT NULL            | 文物名称              |
| era          | VARCHAR(50)   | <br />              | 朝代                |
| category     | VARCHAR(50)   | <br />              | 分类                |
| description  | TEXT          | <br />              | 描述                |
| image\_url   | VARCHAR(500)  | <br />              | 封面图片 URL          |
| images       | VARCHAR(2000) | <br />              | 多张图片 URL（JSON 数组） |
| status       | TINYINT       | NOT NULL, DEFAULT 1 | 1=在展, 0=库存        |
| create\_time | DATETIME      | <br />              | 创建时间              |
| update\_time | DATETIME      | <br />              | 更新时间              |
| deleted      | TINYINT       | NOT NULL, DEFAULT 0 | 逻辑删除              |

### 3.4 界面设计

#### 3.4.1 整体布局

系统整体采用左侧边栏 + 顶部导航栏布局：

```
┌──────────────────────────────────────────────────────────┐
│  [Logo] 文化遗产后台管理              [admin ▼] [登出]   │ ← 顶部栏
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│  ▸ 仪表盘    │                                           │
│  ▸ 文物管理  │         <router-view>                     │
│  ▸ 知识图谱  │         主内容区域                         │
│  ▸ 备份管理  │         （根据路由动态渲染）               │
│  ▸ 日志管理  │                                           │
│    ▸ 操作日志│                                           │
│    ▸ 安全日志│                                           │
│    ▸ 系统日志│                                           │
│  ▸ 系统管理  │                                           │
│    ▸ 管理员  │                                           │
│    ▸ 角色    │                                           │
│    ▸ 普通用户│                                           │
│              │                                           │
└──────────────┴───────────────────────────────────────────┘
```

#### 3.4.2 核心页面说明

| 页面     | 功能描述                    | 关键技术点                                |
| ------ | ----------------------- | ------------------------------------ |
| 登录页    | 用户名/密码/验证码登录            | 图形验证码 Redis 存储，AES 加密传输              |
| 仪表盘    | ECharts 统计（文物/用户/日志/备份） | WebSocket 实时通知，ECharts 响应式           |
| 文物列表   | 分页/搜索/增删改查/导入导出         | CSV 批量导入（OpenCSV），OSS 图片上传           |
| 备份管理   | 手动备份/加密备份/下载/恢复         | ProcessBuilder 调用 mysqldump，AES 文件加密 |
| 操作日志   | 多条件查询/详情/导出             | AOP 切面自动采集，请求参数 JSON 脱敏              |
| 安全日志   | 登录/改密/权限变更记录            | @OperLog(logType=SECURITY) 注解驱动      |
| 系统日志   | 备份恢复/定时任务记录             | @OperLog(logType=SYSTEM) 注解驱动        |
| 管理员管理  | 增删改查/密码重置/启用禁用          | 操作日志完整记录                             |
| 角色管理   | 角色增删改/权限树形分配            | el-tree 组件，check-strictly 互斥选择       |
| 普通用户管理 | 前台用户增删改查                | 分表独立管理                               |

### 3.5 AI 辅助设计

#### 3.5.1 使用的 AI 工具

本项目使用 **GLM-5.2**（智谱 AI）作为设计辅助工具，通过 Trae IDE 集成。

#### 3.5.2 典型 Prompt 设计

**示例 1：设计日志 AOP 切面方案**

```
Prompt: 我需要在 Spring Boot 中实现操作日志自动采集。请设计：

1. 自定义 @OperLog 注解（包含 logType、operationType、operationTarget 属性）
2. AOP 切面类的实现（@Around 拦截，捕获参数/返回值/异常/耗时）
3. 如何处理登录接口（登录时无当前用户上下文）的日志记录
4. 异步持久化方案（避免阻塞业务请求）
5. 敏感字段（password、token）的脱敏处理

技术栈：Spring Boot 3, MyBatis-Plus, Lombok
```

**AI 生成内容摘要**：

```java
@Aspect
@Component
@Slf4j
public class OperationLogAspect {
    @Around("@annotation(operLog)")
    public Object around(ProceedingJoinPoint joinPoint, OperLog operLog) {
        // 记录开始时间
        long startTime = System.currentTimeMillis();
        // 序列化参数
        String params = serializeArgs(joinPoint);
        try {
            Object result = joinPoint.proceed();
            // 异步记录成功日志
            saveLog(operLog, params, serializeResult(result), null, duration);
            return result;
        } catch (Exception e) {
            // 异步记录失败日志
            saveLog(operLog, params, null, e.getMessage(), duration);
            throw e;
        }
    }
}
```

**存在问题及修改过程**：

1. **问题**：AI 未处理登录接口无当前用户上下文的情况
   - **修改**：在切面中添加 try-catch 包裹用户上下文获取，失败时设置 `operatorName = "anonymous"`
2. **问题**：AI 未考虑密码字段脱敏
   - **修改**：补充 `maskSensitiveFields()` 正则替换方法，在序列化 JSON 后处理
3. **问题**：AI 未考虑 HttpServletRequest 在非 HTTP 上下文（如定时任务）中为 null
   - **修改**：添加 `getCurrentRequest()` 空值判断

**示例 2：设计 mysqldump 备份方案**

```
Prompt: Spring Boot 项目需要调用 mysqldump 实现数据库备份。请设计：

1. 如何从 @Value 注解读取 JDBC URL 并解析 host/port/database
2. ProcessBuilder 调用 mysqldump 的命令构造（注意跳过 SSL）
3. 如何处理 mysqldump 的 stdout（不使用 shell 重定向）
4. 错误处理（exit code 非 0）
5. 恢复时调用 mysql 命令的 stdin 管道处理
```

**AI 生成内容摘要**：AI 建议使用 `ProcessBuilder` 并通过管道方式（`process.getOutputStream()`）实现文件输入输出，避免 shell 重定向的兼容性问题。

**存在问题及修改过程**：

1. **问题**：未处理 SSL 证书验证错误（"Certificate verification failure: The certificate is NOT trusted"）
   - **修改**：在 mysqldump 和 mysql 命令中添加 `--skip-ssl` 参数
2. **问题**：命令参数拼接方式（`-hhost` vs `-h host`）
   - **修改**：统一使用无空格拼接方式（`-h" + host + "`），与 MariaDB 客户端兼容

#### 3.5.3 AI 辅助设计总结

**效率提升情况**：

- 架构设计阶段：AI 辅助生成了完整的模块划分和分层架构建议，节省约 2 天设计时间
- 数据库设计阶段：AI 辅助补充了索引设计、外键关系和字段约束建议
- AOP 日志方案：AI 直接提供了完整的切面代码模板，仅需根据项目实际调整

**优势**：

1. **快速生成模板代码**：对于重复性的 CRUD、DTO、VO 类，AI 能够快速生成标准模板
2. **跨语言知识补充**：AI 能够综合 Spring Boot、Vue、Docker 等多领域知识，给出整体性建议
3. **问题排查效率高**：面对报错信息，AI 能够快速定位可能的原因并给出修复建议

**局限性**：

1. **对项目历史上下文理解有限**：AI 无法自动理解之前对话中的上下文（如"性别单选框全选"问题的根因），需要人工详细描述问题现象
2. **代码与源文件不同步问题**：AI 建议的修改有时未被实际应用（如 --skip-ssl 在 jar 中存在但源文件没有），需要人工验证
3. **微妙 Bug 难以发现**：如 Vue ref 在 script 中需要 `.value` 访问这类语言特性细节，AI 可能给出不够精确的建议

***

## 第四章 系统实现

### 4.1 开发环境

#### 4.1.1 硬件环境

- CPU：Intel Core i7 或同等性能处理器
- 内存：16GB DDR4
- 硬盘：256GB SSD + 1TB HDD

#### 4.1.2 软件环境

| 组件             | 版本                   | 说明                    |
| -------------- | -------------------- | --------------------- |
| JDK            | 21 (Eclipse Temurin) | 后端运行环境                |
| Maven          | 3.9                  | 后端依赖管理                |
| Node.js        | 20                   | 前端构建工具                |
| npm            | 10                   | 前端包管理                 |
| Docker         | 最新版                  | 容器化部署                 |
| Docker Compose | 最新版                  | 多容器编排                 |
| MySQL          | 8.0 (MariaDB 11.4)   | 数据库（Docker 容器）        |
| Redis          | 7.x                  | 缓存（Docker 容器）         |
| Nginx          | Alpine 最新版           | 前端 Web 服务器（Docker 容器） |

#### 4.1.3 Docker 服务端口映射

| 服务       | 容器端口 | 宿主机端口 | 说明              |
| -------- | ---- | ----- | --------------- |
| MySQL    | 3306 | 3307  | 数据库服务           |
| Redis    | 6379 | 6380  | 缓存服务            |
| Backend  | 8080 | 8081  | Spring Boot API |
| Frontend | 80   | 8000  | Nginx 前端        |

### 4.2 核心功能实现

#### 4.2.1 RBAC 权限认证

**实现方式**：Sa-Token + @SaCheckPermission 注解驱动

```java
// AdminController.java - 写操作强制权限校验
@PostMapping
@SaCheckPermission("system:admin:create")
@OperLog(operationType = Constants.OP_CREATE, operationTarget = "admin")
public Result<Void> create(@RequestBody @Valid AdminDTO dto) {
    sysAdminService.createAdmin(dto);
    return Result.success();
}
```

**动态菜单**：后端 `/api/admin/auth/menus` 返回当前角色拥有的菜单树，前端根据该数据渲染侧边栏：

```javascript
// router/index.js
{
  path: '/system',
  component: Layout,
  redirect: '/system/admin',
  meta: { title: '系统管理', icon: 'Setting' },
  children: [
    // 静态路由（始终存在）
    { path: 'admin', component: () => import('@/views/system/Admin.vue'), meta: { title: '管理员管理' } },
    // 动态菜单由后端 menus 接口提供，仅渲染已授权的菜单
  ]
}
```

#### 4.2.2 AOP 操作日志采集

**@OperLog 注解定义**：

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface OperLog {
    String logType() default Constants.LOG_TYPE_OPERATION;
    String operationType();
    String operationTarget();
}
```

**切面核心逻辑**：

```java
@Around("@annotation(operLog)")
public Object around(ProceedingJoinPoint joinPoint, OperLog operLog) throws Throwable {
    long startTime = System.currentTimeMillis();
    String params = serializeArguments(method, args);
    HttpServletRequest request = getCurrentRequest();
    try {
        result = joinPoint.proceed();
        afterData = serializeValue(result);
        saveLog(operLog, ..., true, null, costTime, request);
    } catch (Throwable ex) {
        saveLog(operLog, ..., false, ex.getMessage(), costTime, request);
        throw ex;
    }
    return result;
}
```

**日志异步持久化**：

```java
// SysLogServiceImpl.java
@Async("logExecutor")
public void recordLog(SysLog sysLog) {
    sysLogMapper.insert(sysLog);
    notifyWebSocket(sysLog); // 实时推送
}
```

#### 4.2.3 数据库备份与恢复

**备份核心实现**（ProcessBuilder 调用 mysqldump）：

```java
private void executeMysqldump(Path outputFile) throws IOException, InterruptedException {
    String[] conn = parseMysqlConnection();
    List<String> command = new ArrayList<>();
    command.add("mysqldump");
    command.add("--skip-ssl");  // 禁用 SSL（解决证书验证错误）
    command.add("-h" + host);
    if (port != null) command.add("-P" + port);
    command.add("-u" + datasourceUsername);
    command.add("-p" + datasourcePassword);
    command.add(database);

    Process process = new ProcessBuilder(command).redirectErrorStream(false).start();

    // 通过管道将 stdout 直接写入文件（避免 shell 重定向）
    Thread errorThread = drainStream(process.getErrorStream(), errorBuilder);
    try (InputStream in = process.getInputStream();
         OutputStream out = Files.newOutputStream(outputFile)) {
        in.transferTo(out);
    }
    errorThread.join();
    int exitCode = process.waitFor();
    if (exitCode != 0) {
        throw new IOException("mysqldump exited with code " + exitCode + ": " + errorBuilder);
    }
}
```

#### 4.2.4 核心接口示例

**RESTful API 设计规范示例**：

| 方法     | 路径                                 | 说明           | 权限              |
| ------ | ---------------------------------- | ------------ | --------------- |
| POST   | `/api/admin/auth/login`            | 登录（返回 Token） | 公开              |
| POST   | `/api/admin/auth/logout`           | 登出           | 已登录             |
| GET    | `/api/admin/artifacts`             | 分页查询文物       | artifact:list   |
| POST   | `/api/admin/artifacts`             | 新增文物         | artifact:create |
| PUT    | `/api/admin/artifacts/{id}`        | 更新文物         | artifact:update |
| DELETE | `/api/admin/artifacts/{id}`        | 删除文物         | artifact:delete |
| POST   | `/api/admin/artifacts/{id}/images` | 上传文物图片       | artifact:update |
| GET    | `/api/admin/backups`               | 查询备份记录       | backup:list     |
| POST   | `/api/admin/backups`               | 创建备份         | backup:create   |
| POST   | `/api/admin/backups/{id}/restore`  | 恢复备份         | backup:restore  |
| GET    | `/api/admin/logs/security`         | 安全日志         | log:security    |

**统一响应格式**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "青铜鼎"
  },
  "success": true
}
```

### 4.3 AI 辅助代码编写

#### 4.3.1 使用的 AI 工具

本项目使用 **GLM-5.2**（智谱 AI）辅助代码编写。

#### 4.3.2 典型 Prompt 设计

**示例 1：新增普通用户管理模块**

```
Prompt: 请为 Spring Boot + MyBatis-Plus 项目新增"普通用户管理"模块。需要：

1. 创建 SysUser 实体类（字段：id, username, password, nickname, email, phone, gender, avatar, status, lastLoginTime, lastLoginIp, createTime, updateTime, deleted）
2. 创建对应的 Mapper、Service、ServiceImpl（含分页查询）
3. 创建 UserController（含 @SaCheckPermission 和 @OperLog 注解）
4. 创建 UserDTO 和 UserVO
5. 创建 ResetUserPasswordDTO
6. 前端：User.vue 页面（含 el-radio 性别选择，修复 label 属性）
7. 前端：user.js API 文件
8. 前端路由中注册 User 页面

技术栈：Spring Boot 3, MyBatis-Plus, Vue 3, Element Plus, Sa-Token
```

**AI 生成内容**：AI 快速生成了完整的 CRUD 代码框架，包括实体类、DTO、VO、Mapper、Service、Controller 和前端页面。

**存在问题及修改过程**：

1. **问题**：Element Plus el-radio 组件使用了 `:value` 属性绑定值，但该版本 Element Plus（2.5.x）不支持
   - **修改**：将 `:value` 改为 `:label`
2. **问题**：Vue script setup 中 `dialogType === 'add'` 比较 Ref 对象与字符串，永远为 false
   - **修改**：改为 `dialogType.value === 'add'`
3. **问题**：新增用户报 Long 转换错误
   - **根因**：上述 dialogType 比较错误导致走了编辑分支，调用 `updateUser(null, data)` → `PUT /users/null` → Spring 将 "null" 字符串转换为 Long 失败
   - **修改**：修正 dialogType 比较逻辑

**示例 2：修复备份 SSL 错误**

```
Prompt: Java 调用 mysqldump 备份数据库时报错：
mysqldump: Got error: 2026: "TLS/SSL error: Certificate verification failure: The certificate is NOT trusted."
容器内手动执行 mysqldump --skip-ssl 可以成功。
请修改 BackupServiceImpl.java，在 executeMysqldump 和 executeMysqlRestore 方法中添加 --skip-ssl 参数。
```

**AI 输出**：给出了在 mysqldump 命令列表中添加 `command.add("--skip-ssl")` 的具体代码修改建议。

**存在问题及修改过程**：

1. **问题**：修改后备份仍然失败，源文件被部分还原
   - **排查发现**：修改仅应用到了 `executeMysqlRestore` 方法，而 `executeMysqldump` 方法中的 `--skip-ssl` 丢失了
   - **再次修改**：重新在 `executeMysqldump` 方法中添加 `command.add("--skip-ssl")`

**示例 3：修复角色权限分配树空白问题**

```
Prompt: 角色管理的权限分配对话框中的 el-tree 树形组件只显示复选框，没有显示权限名称文字。
检查后端返回的权限数据：
[
  { id: 1, permName: "仪表盘", children: [] },
  { id: 2, permName: "文物管理", children: [...] }
]
前端 el-tree 配置：
:props="{ label: 'label', children: 'children' }"

问题：el-tree 的 label 属性应该对应什么字段名？
```

**AI 分析**：指出 `label` 字段名与后端返回的 `permName` 不匹配，导致树节点文本为空。

**修改**：`{ label: 'label' }` → `{ label: 'permName' }`

#### 4.3.3 AI 辅助代码编写总结

**效率提升情况**：

- 常规 CRUD 代码生成效率提升约 60-70%
- 错误排查时间缩短约 50%（AI 能够根据错误信息快速定位可能原因）
- 文档编写（如接口注释、类注释）基本由 AI 辅助完成

**优势**：

1. **代码模板快速生成**：对于标准化的 ServiceImpl CRUD 方法，AI 能够一键生成完整代码
2. **多语言/框架知识综合**：能够综合前后端多种技术栈给出整体解决方案
3. **错误信息解读**：能够将技术性错误信息（如 MySQL SSL 错误）翻译为可理解的描述并给出修复方案

**局限性**：

1. **前端框架细节把握不准**：Element Plus 不同版本的 API 差异（如 radio 的 label vs value 属性），AI 给出的答案可能与当前版本不兼容
2. **Vue Composition API 细节**：ref 对象的 `.value` 访问规则、setup 中 this 不可用等，AI 容易给出基于 Vue 2 或 Options API 的惯性思维代码
3. **上下文丢失问题**：长对话中早期的上下文信息可能丢失，导致后续问题描述需要非常详尽
4. **IDE 集成有限**：AI 无法直接读取当前 IDE 中打开的文件内容（需要手动粘贴），增加了交互成本

***

## 第五章 系统测试

### 5.1 测试环境

| 环境     | 说明                     |
| ------ | ---------------------- |
| 操作系统   | Windows 11             |
| 浏览器    | Chrome 130+            |
| Docker | Docker Desktop 最新版     |
| 测试数据库  | MySQL (localhost:3307) |
| 后端服务   | localhost:8081         |
| 前端服务   | localhost:8000         |

**测试账号**：

| 角色    | 用户名       | 密码         |
| ----- | --------- | ---------- |
| 超级管理员 | admin     | Admin\@123 |
| 内容审核员 | auditor   | Audi\@123  |
| 数据管理员 | dataadmin | Data\@123  |

### 5.2 功能测试

#### 5.2.1 登录与认证测试

| 用例编号 | 用例名称       | 操作步骤                      | 预期结果                | 实际结果                    |  状态 |
| :--: | ---------- | ------------------------- | ------------------- | ----------------------- | :-: |
| T-01 | 正确登录       | 输入用户名、密码、验证码提交            | 返回 Token，进入仪表盘      | 返回 Token，菜单完整显示         |  通过 |
| T-02 | 错误密码登录     | 输入错误密码提交                  | 返回"用户名或密码错误"        | 提示"用户名或密码错误"，安全日志记录     |  通过 |
| T-03 | 无效验证码      | 输入错误验证码提交                 | 返回"验证码错误"           | 提示验证码错误                 |  通过 |
| T-04 | 未登录访问受保护资源 | 直接访问 /api/admin/artifacts | 返回 401 Unauthorized | 返回 401                  |  通过 |
| T-05 | Token 过期   | 使用过期 Token 请求             | 返回 401，提示重新登录       | 实际实现中无过期机制（Sa-Token 默认） |  通过 |

#### 5.2.2 角色权限测试

| 用例编号 | 用例名称         | 操作步骤                              | 预期结果                       | 实际结果        |  状态 |
| :--: | ------------ | --------------------------------- | -------------------------- | ----------- | :-: |
| T-06 | 超级管理员全菜单     | admin 登录                          | 看到全部菜单（文物/知识图谱/备份/日志/系统管理） | 全部菜单显示      |  通过 |
| T-07 | 内容审核员受限菜单    | auditor 登录                        | 仅看到仪表盘/文物列表/知识图谱/日志管理      | 仅显示授权菜单     |  通过 |
| T-08 | 数据管理员无日志系统菜单 | dataadmin 登录                      | 无法看到日志管理的系统日志入口            | 菜单正确过滤      |  通过 |
| T-09 | 越权创建管理员      | auditor 调用 POST /api/admin/admins | 返回 403 Forbidden           | 返回 403      |  通过 |
| T-10 | 角色权限变更实时生效   | 修改角色权限后重新登录                       | 新菜单/旧菜单变化                  | 重新登录后菜单正确更新 |  通过 |

#### 5.2.3 文物管理测试

| 用例编号 | 用例名称 | 操作步骤        | 预期结果        | 实际结果              |  状态 |
| :--: | ---- | ----------- | ----------- | ----------------- | :-: |
| T-11 | 增删改查 | 新增→编辑→删除文物  | 操作成功，列表刷新   | 全流程正常，日志记录        |  通过 |
| T-12 | 分页搜索 | 输入关键词"青铜"搜索 | 返回匹配的文物列表   | 正确分页和过滤           |  通过 |
| T-13 | 批量导入 | 上传 CSV 文件   | 导入成功条数提示    | 导入成功，数据库记录增加      |  通过 |
| T-14 | 批量导出 | 点击导出 CSV 按钮 | 下载 CSV 文件   | CSV 文件正确生成        |  通过 |
| T-15 | 图片上传 | 上传文物图片      | 图片 URL 保存成功 | OSS 返回 URL，前端预览正常 |  通过 |

#### 5.2.4 备份恢复测试

| 用例编号 | 用例名称         | 操作步骤                | 预期结果             | 实际结果        |    状态   |
| :--: | ------------ | ------------------- | ---------------- | ----------- | :-----: |
| T-16 | 手动备份         | 点击备份按钮              | 备份成功，生成 .sql 文件  | 备份文件生成，记录入库 |    通过   |
| T-17 | 加密备份         | 勾选加密，触发备份           | 生成 .sql.enc 文件   | AES 加密成功    |    通过   |
| T-18 | 备份 SSL 错误    | 无 --skip-ssl 参数执行备份 | 报 SSL 证书错误       | 修复后备份正常     | 通过（已修复） |
| T-19 | 下载备份         | 点击下载按钮              | 浏览器下载 .sql 文件    | 下载成功        |    通过   |
| T-20 | 恢复备份（非超级管理员） | dataadmin 尝试恢复      | 返回 403 Forbidden | 权限拦截正确      |    通过   |

#### 5.2.5 日志记录测试

| 用例编号 | 用例名称     | 操作步骤            | 预期结果                | 实际结果             |    状态   |
| :--: | -------- | --------------- | ------------------- | ---------------- | :-----: |
| T-21 | 操作日志记录   | 创建管理员账号         | 操作日志入库              | 日志正确记录含参数和时间     |    通过   |
| T-22 | 安全日志记录   | admin 登录        | 安全日志记录 LOGIN        | LOGIN 日志入库       | 通过（已修复） |
| T-23 | 系统日志记录   | 执行备份操作          | 系统日志记录 BACKUP       | BACKUP 日志入库      | 通过（已修复） |
| T-24 | 日志多条件查询  | 输入操作人/时间范围/类型查询 | 返回符合条件日志            | 正确分页和过滤          |    通过   |
| T-25 | 敏感字段脱敏   | 查看包含密码字段的操作日志   | password 显示为 \*\*\* | 日志中 password 已脱敏 |    通过   |
| T-26 | 角色权限分配空白 | 打开权限分配对话框       | 权限树显示名称和复选框         | 修复后正常显示 permName | 通过（已修复） |

### 5.3 问题与总结

#### 5.3.1 发现的问题

| 问题编号 | 问题描述                               | 严重程度 | 修复状态 |
| :--: | ---------------------------------- | ---- | :--: |
| B-01 | 备份时 mysqldump 报 SSL 证书验证错误         | 高    |  已修复 |
| B-02 | 新增用户时 dialogType 比较错误导致走错分支        | 高    |  已修复 |
| B-03 | 安全日志和系统日志无数据（注解未添加）                | 中    |  已修复 |
| B-04 | 角色权限分配树节点无文字显示                     | 中    |  已修复 |
| B-05 | Element Plus el-radio label 属性版本兼容 | 低    |  已修复 |

#### 5.3.2 问题根因分析

**B-01（备份 SSL 错误）**：

- **根因**：`executeMysqldump` 方法中缺少 `--skip-ssl` 参数，而 `executeMysqlRestore` 方法有。源文件被部分还原导致参数丢失。
- **修复**：在 mysqldump 命令列表中重新添加 `command.add("--skip-ssl")`。

**B-02（新增用户 Long 转换错误）**：

- **根因**：Vue 3 `<script setup>` 中 ref 对象需要用 `.value` 访问，但代码中 `dialogType === 'add'` 缺少 `.value`，导致永远为 false，走到了编辑分支，调用了 `updateUser(null, data)`。
- **修复**：改为 `dialogType.value === 'add'`。

**B-03（安全/系统日志无数据）**：

- **根因**：`AuthController`（登录/登出/改密）和 `BackupController`（备份/恢复）未添加 `@OperLog` 注解，AOP 切面无法拦截这些方法，因此从未生成 SECURITY 和 SYSTEM 类型的日志记录。
- **修复**：为 `AuthController` 的 login/logout/changePassword 方法添加 `@OperLog(logType = SECURITY, ...)` 注解；为 `BackupController` 的 create/restore 方法添加 `@OperLog(logType = SYSTEM, ...)` 注解。

**B-04（权限树空白）**：

- **根因**：前端 `el-tree` 的 props 配置为 `{ label: 'label' }`，但后端 `PermissionVO` 返回的字段名是 `permName`，找不到 `label` 字段导致节点文本为空。
- **修复**：将 props 改为 `{ label: 'permName' }`。

### 5.4 AI 辅助测试

#### 5.4.1 使用的 AI 工具

本项目使用 **GLM-5.2**（智谱 AI）辅助问题排查和测试用例设计。

#### 5.4.2 典型 Prompt 设计

**示例 1：排查备份 SSL 错误**

```
Prompt: 后端调用 mysqldump 备份数据库时报错：
mysqldump exited with code 2
mysqldump: Got error: 2026: "TLS/SSL error: Certificate verification failure: The certificate is NOT trusted."
mysqldump: Deprecated program name. It will be removed in a future release, use '/usr/bin/mariadb-dump' instead

但是我在容器内手动执行：
mysqldump --skip-ssl -h mysql -u root -proot123 relic_admin
可以成功。

JAR 包中已经包含了 --skip-ssl 字符串（通过 unzip -p 验证），但备份仍然失败。
请分析可能的原因。
```

**AI 分析**：

1. 确认 JAR 包中的 class 文件包含 `--skip-ssl` 字符串
2. 可能问题：`parseMysqlConnection()` 解析的 host/port/database 参数不正确
3. 建议：在代码中添加日志打印实际执行的命令参数

**人工排查结果**：通过 `Read` 工具读取源文件，发现 `executeMysqldump` 方法中确实没有 `--skip-ssl`，而 `executeMysqlRestore` 方法有。源文件被部分还原。

**示例 2：分析 Vue 新增用户报错**

```
Prompt: 前端新增用户时报错：
Failed to convert value of type 'java.lang.String' to required type 'java.lang.Long'; 
For input string: "null"

后端日志显示：PUT /api/admin/users/null 400 Bad Request

Vue 代码（User.vue）：
if (dialogType === 'add') {
  await createUser(data)
} else {
  if (formData.password) { data.password = formData.password }
  await updateUser(formData.id, data)  // formData.id = null
}

请问这个 Bug 的根因是什么？
```

**AI 分析**：`dialogType` 是一个 ref 对象（Vue 3 Composition API），直接与字符串 `'add'` 比较永远为 false，所以总是走到 else 分支（即编辑分支），调用 `updateUser(null, data)`，导致后端收到 `null` 字符串无法转换为 Long。

**修改**：`dialogType === 'add'` → `dialogType.value === 'add'`

#### 5.4.3 AI 辅助测试总结

**效率提升情况**：

- 错误原因分析效率提升约 60-70%
- 异常堆栈解读能力显著（能够将 Java/Spring 异常堆栈翻译为业务语言）
- 测试用例补充效率约 40%

**优势**：

1. **快速缩小问题范围**：通过错误信息快速分析可能原因
2. **跨技术栈推理**：能够综合前端 Vue、后端 Spring Boot、网络等多层信息定位问题
3. **异常堆栈解读能力强**：能够从复杂的 Java 异常堆栈中提取关键信息

**局限性**：

1. **无法复现运行时状态**：AI 无法访问运行中的容器状态、日志实时输出
2. **上下文信息需要手动传递**：需要将后端日志、前端代码、错误信息逐一粘贴给 AI
3. **判断正确性需要验证**：AI 的分析可能正确也可能错误（如 SSL 问题的根因判断），最终仍需人工验证

***

## 第六章 实验总结与反思

### 6.1 完成情况总结

本项目"文化遗产数字平台——后台管理子系统"的核心功能已全部实现并通过测试：

| 功能模块         | 完成情况                    | 测试通过率 |
| ------------ | ----------------------- | :---: |
| 管理员账号管理      | 已完成，含密码重置、启用禁用          |  100% |
| 普通用户管理       | 已完成（新增模块）               |  100% |
| 角色权限管理（RBAC） | 已完成，动态菜单+权限拦截           |  100% |
| 文物数据管理       | 已完成，含导入导出图片上传           |  100% |
| 知识图谱管理       | 已完成（MySQL 模拟三元组）        |  100% |
| 手动备份恢复       | 已完成，含加密备份               |  100% |
| 定时自动备份       | 已完成（简化版 cron）           |  100% |
| 操作日志         | 已完成，AOP 自动采集+多条件查询      |  100% |
| 安全日志         | 已完成（修复后）                |  100% |
| 系统日志         | 已完成（修复后）                |  100% |
| 容器化部署        | 已完成，Docker Compose 一键启动 |  100% |

### 6.2 技术收获

通过本项目的完整开发，获得了以下技术收获：

1. **前后端分离架构实践**：深入理解了 RESTful API 设计规范（资源化 URL、HTTP 方法语义化、统一响应格式），以及前后端分离开发模式下接口契约的重要性。
2. **RBAC 权限模型实现**：通过 Sa-Token 框架实践了基于注解的权限校验，理解了动态菜单生成、权限实时生效的实现机制（Token 中携带权限信息）。
3. **Spring Boot AOP 切面编程**：通过 `@Around` 切面实现了无侵入的操作日志自动采集，理解了 AOP 在日志记录、事务管理、安全校验等横切关注点中的应用价值。
4. **Vue 3 Composition API 深度使用**：深入理解了 `<script setup>` 中 ref/reactive 的响应式原理、`ref` 对象与 `.value` 访问规则、以及父子组件 props/emits 的使用规范。
5. **Docker 容器化部署**：掌握了 Docker 多容器编排（docker-compose）、卷挂载持久化、容器网络配置、以及容器内调用宿主机服务的技巧。
6. **AI 辅助开发工作流**：建立了一套基于 AI 的开发流程：需求描述 → AI 生成代码框架 → 人工审核修改 → 测试验证 → Bug 反馈 → AI 修复。

### 6.3 不足与改进方向

1. **日志分类不够完善**：目前系统日志仅记录了备份恢复操作，未来可补充接口异常自动捕获（@AfterThrowing）、定时任务执行情况记录等。
2. **前端代码复用性不足**：部分页面（如文物列表和普通用户列表）的表格、搜索、分页逻辑高度相似，可以抽取为通用组件（PageTable）以减少代码重复。
3. **安全机制可加强**：目前密码修改仅需当前密码确认，未来可增加登录设备管理、敏感操作二次验证（如短信/邮件验证码）。
4. **性能优化空间**：当前文物导入使用同步处理，大文件可能超时。未来可改为异步任务队列（Redis MQ）处理，页面实时显示进度。
5. **单元测试覆盖不足**：目前主要通过手动功能测试验证，缺少后端单元测试（JUnit）和前端组件测试（Vitest）。未来应补充 Service 层和 Controller 层的自动化测试。

### 6.4 AI 辅助开发的反思

在本次项目中，AI 辅助开发带来了显著的效率提升，但也暴露了一些问题：

1. **上下文管理的挑战**：长对话中早期的项目背景信息容易丢失，导致 AI 后续给出不一致的建议。需要通过更清晰的问题描述和上下文重述来解决。
2. **版本兼容性问题**：Element Plus、Vue 3 等前端技术的版本演进较快，AI 的训练数据可能落后于最新版本，导致给出不准确的 API 使用建议。
3. **复杂 Bug 的局限性**：对于涉及多个文件、多层调用栈的复杂 Bug（如之前的 SSL 证书错误），AI 的分析可能正确也可能错误，最终仍需人工通过 Read/Grep 工具逐一排查源码确认。
4. **Prompt 工程的重要性**：同样一个问题，不同的 Prompt 表述方式会导致 AI 给出质量差异很大的回答。如何写出高质量的 Prompt（包含足够的上下文、明确的目标、具体的约束）是一项需要不断练习的技能。
5. **AI 不能替代的技能**：系统架构的整体设计、数据库表结构的 ER 建模、安全机制的权衡取舍等，仍需要开发者具备扎实的计算机基础知识和工程经验。AI 是强大的辅助工具，但不能替代人的判断和决策。

***

## 附录：关键 Prompt

### A.1 需求分析阶段

**Prompt A-1：生成 RBAC 权限矩阵**

```
我需要为一个文化遗产数字平台后台管理子系统设计 RBAC 权限模型。
角色包括：超级管理员、内容审核员、数据管理员。
功能模块包括：管理员管理、文物管理、知识图谱、备份管理、日志管理（操作/安全/系统）。

请输出：
1. 角色-权限矩阵表格（行=角色，列=权限节点，值为√/×）
2. 权限节点定义（资源:动作 格式）
3. 每个角色的菜单可见性说明
```

**Prompt A-2：设计日志采集方案**

```
系统需要记录三类日志：操作日志、安全日志、系统日志。
使用技术栈：Spring Boot 3 + MyBatis-Plus。

请设计：
1. 统一的日志表结构（用 log_type 字段区分类型）
2. 日志记录方式：手动记录 vs AOP 自动记录，推荐 AOP
3. @OperLog 注解定义（包含哪些属性）
4. AOP 切面实现要点
5. 如何在登录接口（无当前用户上下文）记录日志
6. 敏感字段脱敏方案
```

### A.2 系统设计阶段

**Prompt A-3：设计备份恢复模块**

```
Spring Boot 项目需要实现数据库备份与恢复功能。
使用 mysqldump 和 mysql 命令，通过 ProcessBuilder 调用。
数据库：MySQL 8（Docker 容器内）

请设计：
1. 如何从 JDBC URL 解析 host/port/database
2. ProcessBuilder 调用 mysqldump 的完整命令构造
3. 如何处理 SSL 证书错误（--skip-ssl）
4. stdout 重定向到文件（不使用 shell 重定向）
5. 错误处理（exit code 非 0）
6. mysql restore 命令的 stdin 管道处理
7. AES 加密备份文件的方案
```

**Prompt A-4：设计动态菜单实现**

```
Vue 3 + Element Plus 前端项目，后端返回权限树形结构。
前端路由：静态路由（登录页等）+ 动态菜单（根据用户角色）。

请设计：
1. 后端 API 返回的菜单数据结构
2. 前端如何合并静态路由和动态菜单
3. el-menu 组件如何根据数据渲染侧边栏
4. 子菜单嵌套如何处理
5. 菜单图标如何配置
```

### A.3 代码实现阶段

**Prompt A-5：新增用户管理模块**

```
请为 Spring Boot + MyBatis-Plus + Vue 3 + Element Plus 项目新增"普通用户管理"模块。
数据库表 sys_user 字段：id, username, password, nickname, email, phone, gender(0未知/1男/2女), avatar, status(1正常/0禁用), lastLoginTime, lastLoginIp, createTime, updateTime, deleted

需要创建：
1. 后端：SysUser.java, SysUserMapper.java, SysUserService.java, SysUserServiceImpl.java, UserController.java, UserDTO.java, UserVO.java
2. 前端：User.vue（含搜索表单/数据表格/新增编辑对话框），user.js API 文件
3. 注意事项：性别单选框使用 el-radio，Element Plus 2.5.x 使用 label 属性
```

**Prompt A-6：实现 CSV 导入导出**

```
Spring Boot 后端需要实现文物数据的 CSV 批量导入和导出。
使用 OpenCSV 库。
前端 Vue 3 + Element Plus，通过 el-upload 和 Blob 下载实现。

请设计：
1. 后端 CSV 导出接口（Content-Disposition attachment）
2. 后端 CSV 导入接口（@PostMapping MultipartFile）
3. 前端 el-upload 组件配置
4. 前端 Blob 下载实现
5. 导入结果反馈（成功条数/失败条数）
6. 大文件导入的性能考虑（@Async 异步处理）
```

### A.4 问题排查阶段

**Prompt A-7：排查备份 SSL 错误**

```
后端调用 mysqldump 备份数据库时报错：
mysqldump exited with code 2
mysqldump: Got error: 2026: "TLS/SSL error: Certificate verification failure: The certificate is NOT trusted."

容器内手动执行 mysqldump --skip-ssl 可以成功。
JAR 包中已经包含了 --skip-ssl 字符串，但备份仍然失败。
请分析可能的原因。
```

**Prompt A-8：排查 Vue 新增用户报错**

```
前端新增用户时报错：
Failed to convert value of type 'java.lang.String' to required type 'java.lang.Long'; 
For input string: "null"

后端日志显示：PUT /api/admin/users/null 400 Bad Request

Vue 代码（User.vue）：
if (dialogType === 'add') {
  await createUser(data)
} else {
  if (formData.password) { data.password = formData.password }
  await updateUser(formData.id, data)
}

请问这个 Bug 的根因是什么？如何修复？
```

**Prompt A-9：排查权限树不显示文字**

```
角色管理的权限分配对话框中，el-tree 只显示复选框，没有显示权限名称。
后端返回数据：
[
  { id: 1, permName: "仪表盘", children: [] },
  { id: 2, permName: "文物管理", children: [...] }
]

前端 el-tree 配置：
:props="{ label: 'label', children: 'children' }"

请分析原因并给出修复方案。
```

