**项目名称**：文化遗产数字平台——后台管理子系统

**技术栈**：\
Java21 + SpringBoot3 + SpringMVC + Spring-AOP + MyBatis-Plus + MySQL + Redis + Sa-Token + WebSocket + Lombok + PageHelper + OpenCSV + 阿里云OSS\
Vue3 + Vite6 + Element Plus + Vue Router + Axios + ECharts + Pinia\
**API 风格**：严格遵循 **RESTful** 规范\
部署方式：Docker + Docker Compose 一键容器化部署

***

## 一、需求概述

编写 Web 端后台管理程序（前后端分离），对平台全部数据、用户、内容与系统运行状态进行统一管理，确保平台安全、稳定、高效运行。\
后台系统设置独立的管理员账号体系，与前台用户账号分离（分表存储）。\
**简化版实现范围**：

- 必须实现：角色权限管理、文物数据管理、手动全量备份与恢复、操作日志记录与查询。
- 可选/简化：知识图谱三元组在线编辑可仅用 MySQL 表模拟（不强制接图数据库）；定时自动备份与备份加密可演示级实现（固定密钥 AES 加密）；WebSocket 仅用于管理员实时通知，非强制；安全日志中的“敏感数据访问”定义为密码修改、权限变更、备份恢复等操作。

***

## 二、功能需求

### 1. 角色与权限管理

基于 RBAC 模型，至少三类角色：

- **超级管理员**：全部操作权限，可创建/管理管理员账号、系统配置。
- **内容审核员**：仅内容审核权限，无法操作用户数据与系统配置。
- **数据管理员**：文物数据增删改查权限，无法操作用户账号与系统日志。

要求：

- 动态菜单生成，前端根据角色权限展示不同导航。
- 权限变更后实时生效（利用 Sa-Token 权限认证）。
- 管理员账号密码加密存储。

### 2. 数据管理

#### （1）文物数据管理

- 对文物数据进行增删改查，支持分页列表、关键字搜索。
- 支持批量导入/导出（CSV 格式，使用 OpenCSV）。
- 图片上传至阿里云 OSS，支持批量上传与替换，前端可预览。
- 数据表字段至少包含：文物名称、年代、类别、描述、图片URL、创建时间、更新时间。

#### （2）知识图谱管理（简化版可选）

- 三元组数据（实体-关系-实体）的添加、修改、删除，在线编辑使用 MySQL 关系表存储。
- 修改后提供触发图数据库同步的模拟接口（若未对接真实图数据库，可仅记录日志）。

### 3. 数据备份与恢复

#### （1）手动备份

- 管理员可触发全量备份（导出 SQL 文件），备份文件存储于宿主机挂载卷，同时记录备份信息到数据库。
- 备份时显示进度提示，完成后页面刷新备份记录列表。

#### （2）定时自动备份（可简化）

- 支持配置 cron 表达式，每日凌晨自动全量备份（演示可用固定 2 分钟间隔测试）。
- 备份文件可选进行 AES 加密（固定密钥演示）。

#### （3）备份记录管理

- 列表展示：备份时间、备份类型（全量/增量，简化版可全为全量）、文件大小、操作人、是否加密。
- 支持下载备份文件。

#### （4）数据恢复

- 管理员选择一条备份记录，触发恢复操作。
- 恢复前须经超级管理员二次确认（输入密码或弹窗确认）。
- 恢复过程记录详细日志（操作人、时间、恢复点、结果）。
- 恢复期间系统进入维护状态提示。

### 4. 日志管理

#### （1）操作日志

- 记录管理员的所有增删改查操作，字段包括：操作人ID、操作时间、IP地址、操作类型（如：CREATE/UPDATE/DELETE/LOGIN）、操作对象（表名或功能模块）、操作前/后数据（JSON 格式）。
- 支持按时间范围、操作人、操作类型、关键字多条件组合查询，并分页展示。

#### （2）系统日志

- 记录接口异常（5xx错误）、定时任务执行情况（成功/失败）。
- 可简单通过 AOP 切面捕获异常并入库。

#### （3）安全日志

- 记录登录成功/失败、权限变更、敏感操作（密码重置、备份恢复等）。
- 可与操作日志共用表，通过日志类型字段区分。

#### （4）日志查询与审计

- 提供多维检索界面，支持导出查询结果为 CSV。

***

## 三、非功能需求

- **RESTful API 设计规范**（核心要求）：
  - 资源化 URL：例如 `/api/admin/artifacts` 表示文物资源，`/api/admin/backups` 表示备份记录资源。
  - 严格使用 HTTP 方法：GET（查询）、POST（新增）、PUT（全量更新）或 PATCH（局部更新）、DELETE（删除）。
  - 统一 JSON 响应格式：`{ "code": 200, "message": "success", "data": {} }`，包含适当的 HTTP 状态码（200、201、400、403、500等）。
  - 权限校验通过 Sa-Token 在拦截器层实现，敏感操作需在请求头携带 Token。
- **前后端分离**：Vue3 独立前端工程，通过 Axios 调用后端 RESTful API，前端路由守卫控制页面访问。
- **容器化部署**：提供 Dockerfile 与 docker-compose.yml，至少包含后端服务、MySQL、Redis、前端 Nginx 四个服务。数据库文件、备份文件均通过卷挂载持久化。
- **安全**：密码 BCrypt 加密，敏感操作日志记录，防止 SQL 注入（MyBatis-Plus 参数化查询）。
- **用户体验**：Element Plus 加载状态、操作确认弹窗、分页组件。

***

## 四、交付要求

### 1. 文档提交（三份独立文档，内容不大量重复）

**① 需求分析报告**

- 包含项目背景、功能需求用例建模（用例图 + 用例描述表）、非功能需求、数据字典。

**② 设计报告**

- 系统架构图（含技术选型说明）、模块划分、数据库 ER 图及核心表结构、**RESTful 接口设计**（至少列出 6 个主要 API，需包含 URL、HTTP 方法、请求体/参数示例、响应体示例，并体现资源嵌套关系，如 `/api/admin/artifacts/{id}/images`）。
- RBAC 权限模型设计、备份恢复流程设计、日志存储策略。

**③ 测试报告**

- 测试环境说明、功能测试用例表（至少 15 条，含操作步骤与预期结果）、测试结果截图、缺陷记录与修复情况。
- 每条用例需覆盖正常路径与异常边界（如权限不足、备份恢复二次确认取消等），并针对 RESTful API 设计一条验证 HTTP 方法正确使用的用例（例如：用 POST 创建资源，用 PUT 更新资源等）。

### 2. 可运行简化版系统交付

- 代码 Git 仓库地址或压缩包，含 README 启动说明。
- Docker 一键启动脚本有效，访问 `http://localhost` 可进入后台登录页。
- **内置初始数据**：
  - 超级管理员账号：admin / Admin\@123
  - 内容审核员账号：auditor / Audi\@123
  - 数据管理员账号：dataadmin / Data\@123
  - 预置 15 条文博测试数据。
- **可演示核心流程**：
  - 不同角色登录后菜单差异与权限拦截验证。
  - 文物数据的增删改查、批量导入导出、图片上传（通过 RESTful API 操作）。
  - 一次手动全量备份 → 查看备份记录 → 下载 → 使用该备份恢复数据库（可新增一条数据后恢复验证）。
  - 操作日志记录与多条件查询。

### 3. 评分要点提示

- RESTful API 设计的规范性与一致性。
- 角色权限控制的完整性与安全性。
- 备份恢复流程的健壮性（二次确认、恢复日志、错误处理）。
- 日志的详细程度与查询性能。
- 文档质量：结构清晰、图表规范、体现个人理解。
- 容器化部署的易用性。

***

**附件说明**：\
可根据需要自行选用辅助工具/库，需保证最终可运行。项目结构、代码规范、注释完整度也将纳入评价。
